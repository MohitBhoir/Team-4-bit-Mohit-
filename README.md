In this we have used the pre-trained keras_ocr model for medicine name detection on the packets. 
The user can easily upload the medicine packet image and the keras_ocr model will detect the name.
For web-scraping we have used the BeautifulSoup module in python to scrape the medicine information of the user uploaded image. 
Additionally it will also recommend the users the alternate medicines.
